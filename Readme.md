# Conway's game of life

for more read wikipedia

### How to run on your local system

1. create virtual environment using `venv`, and activate it 
2. Run `pip install -r requirements.txt`, to install the requirements
3. Run `python ./src/main.py` to run the game

> Note : Make sure that SDL is installed on your system.